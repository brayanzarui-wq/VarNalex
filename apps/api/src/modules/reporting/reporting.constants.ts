/**
 * Clave del módulo de negocio "Reportes Automáticos". Se usa con
 * `@RequireModule(REPORTING_MODULE_KEY)` para gatear las rutas por feature flag
 * (sección 5, regla 4). Debe coincidir con la clave usada al activar el módulo.
 */
export const REPORTING_MODULE_KEY = 'reporting';
